<?xml version="1.0" encoding="utf-8"?>
<!--
  Pre-Android-12 splash background. Solid Vaulta crimson so the
  flash between the launch icon and the first frame of the
  security gate stays on brand. The Dart `_OnboardingHero` then
  paints the same crimson gradient with the lock-V glyph on top.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/vaulta_crimson" />
</layer-list>
